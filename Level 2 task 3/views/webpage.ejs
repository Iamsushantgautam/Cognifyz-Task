<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Enhance CSS styling and make the
        webpage fully responsive</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        /* Global Styles */
        body {
            font-family: 'Poppins', sans-serif;
            background: #f0f8ff;
            color: #333;
        }

        h1, h2 {
            color: #005f99;
        }

        /* Hero Section */
        .hero {
            background: linear-gradient(135deg, #6db3f2, #1e69e5);
            color: white;
            text-align: center;
            padding: 60px 0;
            margin-bottom: 50px;
            animation: fadeIn 2s ease-in-out;
        }

        .hero h1 {
            font-size: 48px;
            font-weight: 600;
        }

        .hero p {
            font-size: 20px;
        }

        /* Form Section */
        .form-container {
            background: rgba(255, 255, 255, 0.7);
            border-radius: 12px;
            padding: 40px;
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.15);
            margin-bottom: 50px;
            animation: slideUp 1s ease-out;
        }

        .form-container h2 {
            color: #1e69e5;
            margin-bottom: 30px;
        }

        .form-container input, .form-container textarea, .form-container select {
            width: 100%;
            padding: 12px;
            margin-bottom: 15px;
            border-radius: 8px;
            border: 1px solid #ccc;
            font-size: 16px;
            transition: border 0.3s ease-in-out;
        }

        .form-container input:focus, .form-container textarea:focus {
            border-color: #6db3f2;
            outline: none;
        }

        /* Button */
        .form-container button {
            background: #6db3f2;
            color: white;
            border: none;
            padding: 12px 20px;
            border-radius: 8px;
            cursor: pointer;
            font-size: 18px;
            transition: background-color 0.3s ease;
        }

        .form-container button:hover {
            background-color: #1e69e5;
        }

        /* Footer */
        .footer {
            background-color: #1e69e5;
            color: white;
            text-align: center;
            padding: 30px 20px;
        }

        .footer p {
            font-size: 16px;
        }

        /* Animations */
        @keyframes fadeIn {
            0% { opacity: 0; }
            100% { opacity: 1; }
        }

        @keyframes slideUp {
            0% { opacity: 0; transform: translateY(50px); }
            100% { opacity: 1; transform: translateY(0); }
        }

        /* Media Queries for Responsiveness */
        @media (max-width: 768px) {
            .hero h1 {
                font-size: 36px;
            }

            .form-container {
                padding: 30px;
            }

            .footer p {
                font-size: 14px;
            }
        }
    </style>
</head>

<body>

    <!-- Hero Section -->
    <section class="hero">
        <h1>Welcome to Our Website</h1>
        <p>We provide amazing services to our customers. Join us now!</p>
    </section>

    <!-- Form Section -->
    <section class="container form-container">
        <h2>Register Here</h2>
        <form >
            <label for="username">Username:</label>
            <input type="text" name="username" id="username" required>

            <label for="email">Email:</label>
            <input type="email" name="email" id="email" required>

            <label for="age">Age:</label>
            <input type="number" name="age" id="age" required>

            <label for="bio">Short Bio:</label>
            <textarea name="bio" id="bio" rows="4" required></textarea>

            <button type="submit">Submit</button>
        </form>
    </section>

    <!-- Footer Section -->
    <section class="footer">
        <p>© 2025 All rights reserved.</p>
    </section>

    <!-- Bootstrap JS and dependencies -->
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.min.js"></script>
</body>
</html>
